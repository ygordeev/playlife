export enum Complexity {
  Easy = 'easy',
  Medium = 'medium',
  Hard = 'hard',
}

export enum TaskStatus {
  Backlog = 'Backlog',
  InProgress = 'InProgress',
  Blocked = 'Blocked',
  Completed = 'Completed',
}
